const MUSCLE_GROUPS = [
    "Chest", "Triceps", "Back", "Biceps", "Calves", "Quads",
    "Glutes", "Forearms", "Hamstrings", "Shoulders", "Abs"
  ];
  
  const EQUIPMENT_TYPES = [
    "Bodyweight", "Barbell", "Machine", "Dumbbell", "Cable", "Smith Machine"
  ];
  
  const WORKOUT_STATES = ["Active", "Completed"];
  
  const PROGRAM_STATES = ["Creating", "Active"];
  
  const USER_ROLES = ["User", "Admin", "Premium"];
  
  const USER_STATUSES = ["Pending", "Verified", "Approved", "Rejected"];
  
  const THEMES = ["light", "dark"];
  
  const UNITS = ["metric", "imperial"];
  
  const COOKIE_CONSENT = ["accepted", "rejected"];
  
  module.exports = {
    MUSCLE_GROUPS,
    EQUIPMENT_TYPES,
    WORKOUT_STATES,
    PROGRAM_STATES,
    USER_ROLES,
    USER_STATUSES,
    THEMES,
    UNITS,
    COOKIE_CONSENT,
  };
  